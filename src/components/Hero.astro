---
const {HeroImage, AltText, title, subtitle} = Astro.props
---

<div class="text-center py-10">
    <img src={HeroImage} alt={AltText} class="w-[100%] max-h-[400px] object-cover">
    <div class="bg-highlightred sm:rounded-b-md p-2 md:p-4">
        <h1 class="text-3xl md:text-5xl font-bold md:py-4 text-white">{title}</h1>
        <h2 class="text-2xl md:text-4xl text-lightgray">{subtitle}</h2>
    </div>
</div>
